package freecell.model;

/**
 * Enum class to differentiate between the two card colours. This would be helpful in determining
 * the validity of the move when moving between cascade piles.
 */
public enum CardColor {
  RED, BLACK
}
